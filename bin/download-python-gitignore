#!/bin/bash

cat <(curl https://raw.githubusercontent.com/github/gitignore/main/Global/macOS.gitignore) \
    <(printf "\n") \
    <(curl https://raw.githubusercontent.com/github/gitignore/main/Python.gitignore) > .gitignore
