#! /usr/bin/env python3
from pathlib import Path

store_path = Path(Path.home(), '.password-store')
assert store_path.exists(), '~/.password-store/ not found'
gpg_files = (f.relative_to(store_path).with_suffix('') for f in store_path.glob('**/*.gpg'))
for password in gpg_files:
    print(password)
